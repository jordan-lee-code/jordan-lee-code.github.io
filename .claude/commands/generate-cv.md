Run `scripts/generate-cv.sh` to regenerate `assets/Jordan-Lee-CV.pdf` from `assets/cv.html`.

After generating, check if the PDF changed. If it did, stage it and commit with the message "Update CV PDF". If `assets/cv.html` is out of date with the content in `_tabs/about.md`, `_tabs/portfolio.md`, or `_tabs/tech-stack.md`, update `assets/cv.html` first before generating.
